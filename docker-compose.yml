services:
  backend:
    build: .
    container_name: churn-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: churn-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend